#!/usr/bin/env ruby

require_relative '../lib/it_tools/solr'

options = { :solr_host => '127.0.0.1', :solr_port => '8983' }

query = Solr::Query.new options

params = { "query" => "web" }
resp = query.do_query params
p resp
